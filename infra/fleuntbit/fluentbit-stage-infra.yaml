apiVersion: fluentbit.fluent.io/v1alpha2
kind: ClusterInput
metadata:
  labels:
    fluentbit.fluent.io/component: logging
    fluentbit.fluent.io/enabled: "true"
  name: fluentbit-infra-input
spec:
  tail:
    db: /fluent-bit/tail/pos-infra.db
    dbSync: Normal
    memBufLimit: 15MB
    parser: cri
    path: /var/log/containers/*infra*.log
    excludePath: "/var/log/containers/fluent*.log"
    refreshIntervalSeconds: 10
    skipLongLines: true
    tag: infra*
---
apiVersion: fluentbit.fluent.io/v1alpha2
kind: ClusterOutput
metadata:
  name: fluentbit-infra-output
  labels:
    fluentbit.fluent.io/enabled: "true"
    fluentbit.fluent.io/component: logging
spec:
  match: infra*
  alias: opensearch
  opensearch:
    host: vpc-esinfra-qskvjpwkerxwwnwawflh2uvcpe.eu-west-1.es.amazonaws.com
    port: 443
    httpUser:
      valueFrom:
        secretKeyRef: 
          key: user
          name: opensearchsecret
    httpPassword:
      valueFrom:
        secretKeyRef: 
          key: password
          name: opensearchsecret          
    generateID: true
    logstashPrefix: infra
    logstashFormat: true
    timeKey: "@timestamp"
    tls:
      verify: false
--- 
apiVersion: fluentbit.fluent.io/v1alpha2
kind: ClusterFilter
metadata:
  labels:
    fluentbit.fluent.io/component: logging
    fluentbit.fluent.io/enabled: "true"
  name: fluentbit-infra-filter
spec:
  filters:
  - kubernetes:
      annotations: false
      kubeCAFile: /var/run/secrets/kubernetes.io/serviceaccount/ca.crt
      kubeTokenFile: /var/run/secrets/kubernetes.io/serviceaccount/token
      kubeURL: https://kubernetes.default.svc:443
      labels: false
      kubeTagPrefix: infra.var.log.containers.
  - nest:
      addPrefix: kubernetes_
      nestedUnder: kubernetes
      operation: lift
  - modify:
      rules:
      - remove: stream
      - remove: kubernetes_pod_id
      - remove: kubernetes_host
      - remove: kubernetes_container_hash
  - nest:
      nestUnder: kubernetes
      operation: nest
      removePrefix: kubernetes_
      wildcard:
      - kubernetes_*
  match: infra*    